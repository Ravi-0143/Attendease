---
description: Run the attendance app locally so the teacher can take today's attendance by uploading a class photo.
---

// turbo
1. **Start the app**
   Run `python app.py` to start the Flask web server.

2. **Open the browser**
   Open the browser and go to `http://localhost:5000`.
   Verify the home screen loads correctly and the Upload Photo button is visible.

3. **Confirm app is ready**
   Tell me: "Your attendance app is open and ready. Upload today's class photo to begin."
